{
  "name": "@optcg/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./dist/index.js",
    "./protocol": "./dist/protocol.js"
  },
  "scripts": {
    "bench": "tsc -p tsconfig.bench.json && node --expose-gc bench/dist/bench/run.js",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/main.js",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@optcg/cards": "workspace:*",
    "@optcg/engine": "workspace:*",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/ws": "^8.18.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.0"
  }
}
